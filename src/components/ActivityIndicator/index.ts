/**
 * ActivityIndicator Component Entry Point
 *
 * Re-exports the `ActivityIndicator` component and its props for easier imports.
 * This allows consumers to import from the component directory directly:
 * 
 * Example:
 * import { ActivityIndicator, ActivityIndicatorProps } from "@geekyhawks/react-native-ui-components";
 *
 * Author: Geeky Hawks FZE LLC
 */

export { default as ActivityIndicator } from "./ActivityIndicator";
export type { Props as ActivityIndicatorProps } from "./ActivityIndicator";
