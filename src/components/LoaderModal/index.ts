/**
 * LoaderModal Component Entry Point
 *
 * Re-exports the `LoaderModal` component and its props for easier imports.
 * This allows consumers to import from the component directory directly:
 * 
 * Example:
 * import { LoaderModal, LoaderModalProps } from "@geekyhawks/react-native-ui-components";
 *
 * Author: Geeky Hawks FZE LLC
 */

export { default as LoaderModal } from "./LoaderModal";
export type { Props as LoaderModalProps } from "./LoaderModal";
