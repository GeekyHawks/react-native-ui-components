/**
 * StatusBar Component Entry Point
 *
 * Re-exports the `StatusBar` component and its props for easier imports.
 * This allows consumers to import from the component directory directly:
 * 
 * Example:
 * import { StatusBar, StatusBarProps } from "@geekyhawks/react-native-ui-components";
 *
 * Author: Geeky Hawks FZE LLC
 */

export { default as StatusBar } from "./StatusBar";
export type { Props as StatusBarProps } from "./StatusBar";
